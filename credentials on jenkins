manage plugins (install Deploy to war)
go to credentials and add credentials (username deployer passwd deployer)
tomcat url
add tomcat
generate syntax
nxt go to pipeline
stage('deploy to tomcat')
  steps{
paste the generate syntax
    save and apply
  Build Now
    then go to tomcat and refresh once one application is added
    * THANK YOU *      (VAMSI-CREATED-THIS)
